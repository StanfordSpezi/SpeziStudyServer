meta {
  name: Get Studies In Group
  type: http
  seq: 3
}

get {
  url: {{baseUrl}}/groups/{{groupId}}/studies
  body: none
  auth: inherit
}

script:post-response {
  const body = res.getBody();
  if (body && body.length > 0) {
    bru.setVar("studyId", body[0].id);
  }
}
