meta {
  name: Get Groups
  type: http
  seq: 1
}

get {
  url: {{baseUrl}}/groups
  body: none
  auth: inherit
}

script:post-response {
  const body = res.getBody();
  if (body && body.length > 0) {
    bru.setVar("groupId", body[0].id);
  }
}
